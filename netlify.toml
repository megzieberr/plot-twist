[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

# SPA fallback (API routes are claimed by the functions' own path config)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
