# Local dev secrets — copy to .env (never committed).
# TMDB key: https://www.themoviedb.org/settings/api  (server-side only, no VITE_ prefix)
TMDB_API_KEY=

# Supabase (safe to expose to the client — protected by RLS)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
